<?php
// Plugin Manager configuration (dist)

// ===== Core behavior =====
$config['pm_debug'] = false;
$config['pm_remote_checks'] = true;           // query GitHub for remote versions
$config['pm_roundcube_release_channel'] = 'stable'; // 'stable' (final releases only) or 'rc' (include pre-releases)
$config['pm_request_timeout'] = 7;            // HTTP timeout (seconds)
$config['pm_cache_ttl'] = 43200;              // remote check cache TTL (seconds)
$config['pm_plugin_root'] = null;             // override plugins root dir (null = auto)
$config['pm_update_channel'] = 'release';     // 'release' or 'dev'

// ===== Bulk update =====
$config['pm_enable_update_select'] = true;       // show top-level "Update Selected" button

// ===== Allowlisted post-update hooks =====
// Explicit admin allowlist only. Nothing runs unless listed here and enabled.
// Supported type in this phase: 'internal'
// Hook files are owned by Plugin Manager itself and must live under plugin_manager/webhooks/<plugin>/.
$config['pm_post_update_hooks'] = [
  // 'plugin_name' => [
  //   'enabled' => true,                  // master switch for this plugin hook
  //   'type' => 'internal',              // only Plugin Manager-owned internal hook files are supported
  //   'path' => 'post-update.php',       // relative path under plugin_manager/webhooks/<plugin>/
  //   'label' => 'Run post-update maintenance', // optional UI label for maintenance actions
  //   'timeout' => 10, // parsed for validation/logging; not enforced in this phase
  //   'args' => [],
  //   'run_on' => ['install', 'update', 'manual'], // supported events
  //   'allow_manual_run' => true,        // allow manual execution through the backend/manual route
  //   'show_manual_link' => true,       // show the row-level "Run maintenance" link even when auto-run is enabled
  //   'require_confirmation' => false,   // auto-run skipped unless manually confirmed when true
  // ],
  //
  // Example: texxasrulez/calendar
  // Upstream docs note backend-specific SQL initialization under drivers/*/SQL
  // and companion plugin requirements such as libcalendaring/libkolab.
  // 'calendar' => [
  //   'enabled' => true,
  //   'type' => 'internal',
  //   'path' => 'post-update.php',
  //   'label' => 'Review calendar maintenance',
  //   'run_on' => ['update', 'manual'],
  //   'allow_manual_run' => true,
  //   'show_manual_link' => true,
  //   'require_confirmation' => true,
  // ],
  //
  // Example: mstilkerich/rcmcarddav
  // Upstream docs note that database migration happens on the next login
  // and that major upgrades may require admin config review.
  // 'rcmcarddav' => [
  //   'enabled' => true,
  //   'type' => 'internal',
  //   'path' => 'post-update.php',
  //   'label' => 'Review CardDAV maintenance',
  //   'run_on' => ['update', 'manual'],
  //   'allow_manual_run' => true,
  //   'show_manual_link' => true,
  //   'require_confirmation' => true,
  // ],
  //
  // Example: texxasrulez/additional_imap
  // Upstream docs note composer installs inject SQL automatically, while manual installs require SQL import.
  // 'additional_imap' => [
  //   'enabled' => true,
  //   'type' => 'internal',
  //   'path' => 'post-update.php',
  //   'label' => 'Review Additional IMAP maintenance',
  //   'run_on' => ['update', 'manual'],
  //   'allow_manual_run' => true,
  //   'show_manual_link' => true,
  //   'require_confirmation' => true,
  // ],
  //
  // Example: texxasrulez/additional_smtp
  // Upstream docs note composer installs inject SQL automatically, while manual installs require SQL import.
  // 'additional_smtp' => [
  //   'enabled' => true,
  //   'type' => 'internal',
  //   'path' => 'post-update.php',
  //   'label' => 'Review Additional SMTP maintenance',
  //   'run_on' => ['update', 'manual'],
  //   'allow_manual_run' => true,
  //   'show_manual_link' => true,
  //   'require_confirmation' => true,
  // ],
  //
  // Example: pimlie/authres_status
  // Upstream docs recommend reviewing trusted Authentication-Results authserv-id settings.
  // 'authres_status' => [
  //   'enabled' => true,
  //   'type' => 'internal',
  //   'path' => 'post-update.php',
  //   'label' => 'Review authres_status maintenance',
  //   'run_on' => ['update', 'manual'],
  //   'allow_manual_run' => true,
  //   'show_manual_link' => true,
  //   'require_confirmation' => true,
  // ],
  //
  // Example: texxasrulez/identity_switch
  // This example is intentionally conservative and focuses on post-upgrade config review.
  // 'identity_switch' => [
  //   'enabled' => true,
  //   'type' => 'internal',
  //   'path' => 'post-update.php',
  //   'label' => 'Review Identity Switch maintenance',
  //   'run_on' => ['update', 'manual'],
  //   'allow_manual_run' => true,
  //   'show_manual_link' => true,
  //   'require_confirmation' => true,
  // ],
  //
  // Example: texxasrulez/libkolab
  // Review UPGRADING/README and any SQL/bin helpers after upgrade.
  // 'libkolab' => [
  //   'enabled' => true,
  //   'type' => 'internal',
  //   'path' => 'post-update.php',
  //   'label' => 'Review libkolab maintenance',
  //   'run_on' => ['update', 'manual'],
  //   'allow_manual_run' => true,
  //   'show_manual_link' => true,
  //   'require_confirmation' => true,
  // ],
  //
  // Example: texxasrulez/scheduled_sending
  // Upstream docs require queue schema plus a worker or cron call using the configured token.
  // 'scheduled_sending' => [
  //   'enabled' => true,
  //   'type' => 'internal',
  //   'path' => 'post-update.php',
  //   'label' => 'Review Scheduled Sending maintenance',
  //   'run_on' => ['update', 'manual'],
  //   'allow_manual_run' => true,
  //   'show_manual_link' => true,
  //   'require_confirmation' => true,
  // ],
  //
  // Example: texxasrulez/tasklist
  // Review CalDAV/RoundDAV endpoint settings and task list discovery after upgrade.
  // 'tasklist' => [
  //   'enabled' => true,
  //   'type' => 'internal',
  //   'path' => 'post-update.php',
  //   'label' => 'Review Tasklist maintenance',
  //   'run_on' => ['update', 'manual'],
  //   'allow_manual_run' => true,
  //   'show_manual_link' => true,
  //   'require_confirmation' => true,
  // ],
];

// ===== Maintenance audit trail =====
$config['pm_maintenance_audit_enabled'] = true;  // record recent maintenance runs/skips/refusals in data/
$config['pm_maintenance_audit_limit'] = 100;     // maximum number of audit entries to retain

// true = refresh central data/version.json on init; missing data/version.json and data/installed_versions.json are bootstrapped automatically
$config['pm_write_central_on_init'] = true;

// Map specific plugin dirs to GitHub repos (owner/repo or full URL)
$config['pm_sources_map'] = [
  // 'account_details' => 'https://github.com/owner/repo',
];

// Optional: GitHub token to raise API rate limits for remote checks
$config['pm_github_token'] = 'github_pat_YOUR_TOKEN_HERE'; // e.g. 'ghp_xxxxx...'

// Hide backup folders in UI and actions
$config['pm_hide_bak'] = true;

// ===== Admins =====
$config['pm_visibility'] = 'admin_only'; 	  // 'admin_only' or 'mixed' (default)
$config['pm_update_admins'] = [1];            // only these Roundcube user IDs see Update/Restore/Bulk

// ===== UI niceties =====
$config['pm_column_widths'] = [
  'select' 			=> '3%',
  'plugin'  		=> '20%',
  'directory' 		=> '15%',
  'enable_disable' 	=> '10%',
  'latest' 			=> '8%',
  'local'  			=> '8%',
  'status' 			=> '18%',
  'website' 		=> '18%',
];

// ===== Backups & retention =====
$config['pm_backups'] = true;                 // create .bak-YYYYmmdd-HHMMSS before updating
$config['pm_auto_rollback'] = true;           // auto-restore last backup if update throws
$config['pm_keep_backups'] = 3;               // keep newest N backups per plugin (set <1 for unlimited)
$config['pm_backups_max_age_days'] = 30;      // also delete backups older than N days (0 disables)

// ===== Email alerts (lazy cron) =====
$config['pm_notify_enabled'] = true;
$config['pm_notify_recipients'] = ['user@domain.com'];
$config['pm_notify_from'] = 'user@domain.com';
$config['pm_notify_interval_hours'] = 3;

// ===== Policies: pins & ignore (scoped) =====
$config['pm_features_enabled'] = true;        // master switch for optional features
$config['pm_policies_enabled'] = true;

// Pins (highest precedence). Shown with [Pinned vX.Y]; excluded from bulk/alerts.
$config['pm_pins_enabled'] = true;
$config['pm_pins'] = [
  // 'plugin_dir' => 'version',
  // 'account_details' => '3.4',
];
$config['pm_pins_allow_manual'] = true;       // allow manual Update even when pinned

// Ignore rules with scopes: discover, notify, bulk, ui
$config['pm_ignore_enabled'] = true;
$config['pm_ignore_rules'] = [
  // ['plugin' => 'some_plugin', 'scopes' => ['bulk','notify'], 'reason' => 'local patch'],
  // ['pattern' => '/^rcmail_.*/i', 'scopes' => ['ui'], 'reason' => 'core system'],
];

// ===== Webhooks =====
$config['pm_webhook_enabled'] = false;
$config['pm_webhook_url'] = '';               // Slack/Discord/Teams incoming webhook URL
$config['pm_webhook_events'] = ['notify','update','bulk']; // choose events

// ===== Security: checksum enforcement (release channel) =====
$config['pm_require_checksum'] = false;       // require a SHA256 asset for release zips
$config['pm_checksum_asset_regex'] = '/(sha256|checksums).*\.(txt|sha256)$/i';

// ===== Ace editor (themes) =====
// Choose a theme for Ace. Use 'auto' to pick based on system preference.
// NOTE: In this plugin package we ship only 'monokai' locally. Other themes will be loaded from CDN on demand.
$config['pm_ace_theme']       = 'genesworld';     // 'auto' or explicit theme (e.g., 'monokai')
$config['pm_ace_light_theme'] = 'github';  // used when pm_ace_theme = 'auto'
$config['pm_ace_dark_theme']  = 'genesworld';  // used when pm_ace_theme = 'auto'

// ===== Hidden plugins (do not show in UI) =====
// Names must match plugin directory names (case-insensitive).
// Pre-fill with Roundcube core plugins you might not want managed here. These are taken care of by the Roundcube team.
$config['pm_hidden_plugins'] = array(
    'acl',
    'additional_message_headers',
    'archive',
    'attachment_reminder',
    'autologon',
    'autologout',
    'caldav_calendar',
    'database_attachments',
    'debug_logger',
    'emoticons',
    'enigma',
    'example_addressbook',
    'filesystem_attachments',
    'help',
    'hide_blockquote',
    'http_authentication',
    'identicon',
    'identity_select',
    'identity_switch',
    'info_portal',
    'jqueryui',
    'krb_authentication',
    'libcalendaring',
    'libkolab',
    'managesieve',
    'markdown_editor',
    'markasjunk',
    'newmail_notifier',
    'new_user_dialog',
    'new_user_identity',
    'password',
//    'plugin_manager',
    'reconnect',
    'redundant_attachments',
    'roundrive',
    'show_additional_headers',
    'squirrelmail_usercopy',
    'subscriptions_option',
    'userinfo',
    'vcard_attachments',
    'virtuser_file',
    'virtuser_query',
    'zipdownload'
);
